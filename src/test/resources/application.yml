spring:
  profiles:
    active: test

---

spring:
  config:
    activate:
      on-profile: test

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:database
    username: sa
    password:

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: create-drop

  flyway:
    enabled: false

  sql:
    init:
      mode: never

oauth:
  clientId: abcd1234
  redirectUri: http://localhost:3000/hello

cors:
  origin: "https://example.eat-da.com"

jwt:
  secret-key: dGVzdC1zZWNyZXQta2V5LXRlc3Qtc2VjcmV0LWtleS10ZXN0LWtleS10ZXN0LXNlY3JldC1rZXktdGVzdA==
  access-token-expiration: 1h
  refresh-token-expiration: 1d
